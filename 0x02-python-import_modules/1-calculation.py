#!/usr/bin/python3
import calculator_1 as calc
if __name__ == "__main__":
    a = 10
    b = 5
    add = calc.add(a, b)
    sub = calc.sub(a, b)
    mul = calc.sub(a, b)
    div = calc.sub(a, b)
    print("{a} + {b} = {add}")
    print("{a} - {b} = {sub}")
    print("{a} * {b} = {mul}")
    print("{a} / {b} = {div}")
